{
  "name": "functions",
  "private": true,
  "description": "Cloud Functions for Jimmy the Goat — push notifications only (weekly weigh-in reminders + turning any notification doc into a real push).",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^14.4.0",
    "firebase-functions": "^7.4.0"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions"
  }
}
